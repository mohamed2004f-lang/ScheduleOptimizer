[tool.ruff]
target-version = "py310"
line-length = 127
src = ["backend"]

[tool.coverage.run]
source = ["backend"]

[tool.coverage.report]
# بوابة الجودة: درجات / رفع / صلاحيات — لا تخفض دون 40
fail_under = 40
precision = 0
omit = ["*/tests/*"]
